disconnect;;;;
say disconnect;
bind x "say disconnect"
alias disconnect "disconnect; say disconnect"

bind x bind mouse1 "say mouse1"

bind mouse2 " if ('$matchtype' == '2on2') then .lost else if ('$weapon' == '$tp_name_lg') then .left_pack lg- else if ('$weapon' == '$tp_name_rl') then .left_pack rl- else say else disconnect; disconnect "


bind mouse2 "
if ('$matchtype' == '2on2') then
    .lost
else if ('$weapon' == '$tp_name_lg') then
    .left_pack lg-
else if ('$weapon' == '$tp_name_rl') then
    .left_pack rl-
else
    say else disconnect
;
disconnect
"
